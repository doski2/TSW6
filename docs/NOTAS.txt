# Notas personales
- planes de frenado probar y revisar con detalle
- ~~revisar si en la base de datos hay horario~~ → OK: `tsw_hud.db` + arr/dep en GUI (2026-08-24)
- añadir señales de ambar y rojo (semaforos)
- car_stop_signs gps nos hace falta extraer gps del juego para frenar al milimetro
- mandos: usar IPC siempre que `mandos=ipc` en heartbeat (más fiable que teclado A/D)

estudiado documento estado:
1. main api = hud vemos datos driverAid el que ve limites,puertas y demas y getdata recopila info de todo lo que nos manda driverAid
 1.1 reviar que nos puede servir para una mejora 
 1.2 Estaciones y andenes hay que estudiar si uno de los nombre es el que nos mande parar aqui en este punto exacto como el hud del juego
  1.2.1 gps y cordenadas 
 1.3 cameraMode insertar y probar mas adelante
 2. y la parte fisica del juego que datos podemos optener?
 3. 2.º límite (lim2): aparcado. Probe = 1 cartel (escalares). HTTP sí tiene cola;
    lim2 ≠ nextSpeedLimits[1] (a menudo mismo mph). Ver DRIVERAID_API.md.
 4. unificar archivos revisar fluyo de freno y aver que se puede hacer
 5. falta añadir fuerza en lua
 6. numerar los md para poder segui un orden de tareas
 7. 
 8. documentar reglas implementadas (docs ya existentes, no más md sueltos)
 9. canal: A1 en código (medir work); hitch carga OK; SHM no ahora — CANAL_CONTROL.md
 tratas de mejoras y no añadir mas lineas inecesarias ver como funcionana las actuales y pulir/mejorar las reglas utiliza documento ya exsistente? -El archivo que utilizas en 
 Cursor para darle instrucciones globales a la IA sobre tu proyecto se llama .cursorrules